/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    let stack = [];
    let machingBreackets = {
        ')':'(',
         ''
    }
    
};

module.exports = { isValid };


